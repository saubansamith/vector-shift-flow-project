To Execute the project, kindly run the following commands while staying in the respective directory:

# /frontend/
npm install
node start

# /backend/
uvicorn main:app --reload  

To create a handle in Text node, please use double curly braces and the parameter name in between. For example,
{{example_handle}}

Thank You.
